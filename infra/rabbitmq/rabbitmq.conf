## Carrega as definições de filas, exchanges, bindings e usuários na inicialização.
## Este é o método moderno (RabbitMQ 3.7+) e tem precedência sobre RABBITMQ_SERVER_ADDITIONAL_ERL_ARGS.
load_definitions = /etc/rabbitmq/definitions.json
